[user]
  email = info7tik@gmail.com
  name = info7tik
[core]
  editor = vim
[alias]
  aliases = config --get-regexp ^alias
  branch = branch --sort=-
  graph = log --graph --oneline -n 20
  history = log -p --
  last = log -n 1 --stat
  logfile = log -n 5 --
  nlast = log -n 5 --oneline
  resetall = clean -f -d
  staged = diff --name-status --cached
  track = update-index --no-assume-unchanged
  untrack = update-index --assume-unchanged
[branch]
  sort = committerdate
[init]
  defaultBranch = main
